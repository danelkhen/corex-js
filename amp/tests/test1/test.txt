(ctl,data) => res.verify('div')
    res.verify('h1').text("contacts")
    repeater(res, data.contacts)
        contact => res.verify('div.contact')
            res.verify("label").text(contact.name)
            res.verify("input").val(contact.name)
