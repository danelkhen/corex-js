(ctl,data) => el.verify('div')
    el.external("test2", data.contacts[0].name)
    el.verify('h1').text("contacts")
    $("#test").clone().removeAttr("id")
    el.repeater(data.contacts)
        contact => el.verify('div.contact')
            el.columnar()
                el.verify("label").text(contact.name)
                el.verify("input").val(contact.name)
